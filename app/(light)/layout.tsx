// Světlá poloha je od v1.1 výchozí pro celou aplikaci (blush-50 z body).
// Skupina (light) zůstává kvůli struktuře marketingových stránek.
export default function LightLayout({ children }: { children: React.ReactNode }) {
  return children;
}
